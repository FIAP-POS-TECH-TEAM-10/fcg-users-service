{
  "family": "fcg-users-service-task",
  "networkMode": "bridge",
  "containerDefinitions": [
    {
      "name": "fcg-users-service-container",
      "image": "users-service:latest",
      "cpu": 256,
      "memory": 256,
      "essential": true,
      "portMappings": [
        {
          "containerPort": 5001,
          "hostPort": 5001,
          "protocol": "tcp"
        }
      ],
      "healthCheck": {
        "command": [
          "CMD-SHELL",
          "curl -f http://localhost:5001/health/live || exit 1"
        ],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 15
      }
    }
  ],
  "requiresCompatibilities": [
    "EC2"
  ]
}